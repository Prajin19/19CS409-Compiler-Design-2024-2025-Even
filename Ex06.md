# Ex. No : 6	
# IMPLEMENTATION OF THE BACK END OF THE COMPILER 
## Name : Prajin S
## Register Number : 212223230151
## Date : 19-05-2026

## AIM   
To write a program to implement the back end of the compiler.

## ALGORITHM
1.	Start the program.
2.	Get the three variables from statements and stored in the text file k.txt.
3.	Compile the program and give the path of the source file.
4.	Execute the program.
5.	Target code for the given statement is produced.
6.	Stop the program.

## PROGRAM
```C
#include <stdio.h>
#include <ctype.h>
#include <stdlib.h>

int main() {

    int i = 2, j = 0, k = 2, k1 = 0;
    int n;

    char ip[10][20];

    printf("Enter the number of statements: ");
    scanf("%d", &n);

    // Receiving all inputs first
    for(int count = 0; count < n; count++) {
        scanf("%s", ip[count]);
    }

    // Printing heading after input
    printf("\nStatement\tTarget Code\n\n");

    for(int count = 0; count < n; count++) {

        printf("%s\tMOV %c,R%d ", ip[count], ip[count][i + k], j);

        if(ip[count][i + 1] == '+')
            printf("ADD ");
        else
            printf("SUB ");

        if(islower(ip[count][i]))
            printf("%c,R%d\n", ip[count][i + k1], j);
        else
            printf("%c,%c\n", ip[count][i], ip[count][i + 2]);

        j++;
        k1 = 2;
        k = 0;
    }

    return 0;
}
```

## OUTPUT 

<img width="1692" height="914" alt="image" src="https://github.com/user-attachments/assets/7f4301f0-9954-4984-b5d1-684d94fb5164" />


## RESULT
The back end of the compiler is implemented successfully, and the output is verified.
